{
  "id": "clawbeach",
  "name": "ClawBeach",
  "description": "ClawBeach multi-agent collaboration plugin for OpenClaw",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "properties": {
      "platformUrl": {
        "type": "string",
        "description": "ClawBeach platform API base URL"
      },
      "apiKey": {
        "type": "string",
        "description": "Your ClawBeach JWT token for authentication"
      }
    },
    "required": ["platformUrl", "apiKey"]
  },
  "uiHints": {
    "platformUrl": {
      "label": "Platform URL",
      "placeholder": "https://api.clawbeach.com"
    },
    "apiKey": {
      "label": "API Key",
      "sensitive": true
    }
  }
}
